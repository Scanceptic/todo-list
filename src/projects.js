/* Opens up the projects page */
export default function projects() {
	console.log("Loading Project Page...");
	// expect the page contents to be clear > no need to remove any elements on page
	// get projects from storage
	// add projects to page DOM
}

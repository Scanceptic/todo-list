/* Opens up the homepage */
export default function home() {
	console.log("Loading Home Page...");
	// expect the page contents to be clear > no need to remove any elements on page
	// get local storage contents
	// if nothing in storage (first time user) create default project and tasks and send to storage
	// get local storage again
	// add page contents from local storage to DOM
}
